export function Event({ openEnvelope }: any) {
  return (
    <div
      className={`${openEnvelope ? "block" : "hidden"} h-screen content-center text-center font-bold text-green-800 text-3xl`}
    >
      <h1>Bacveccccc</h1>
    </div>
  );
}
